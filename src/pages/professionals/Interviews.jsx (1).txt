import React, { useState } from "react";
import {
  Box,
  Typography,
  Grid,
  Avatar,
  Stack,
  Button,
  Chip,
  Paper,
} from "@mui/material";

const PRIMARY = "#7F42E7";

// fake time slots
const timeSlots = [
  "09:00", "10:00", "11:00",
  "13:00", "14:00", "15:00"
];

const BookingPage = () => {
  const [selectedDate, setSelectedDate] = useState("2026-05-10");
  const [selectedTime, setSelectedTime] = useState("");
  
  return (
    <Box sx={{ py: 4 }}>
      <Grid container>
        
        {/* LEFT SIDE (PROFILE) */}
        <Grid item xs={12} md={4}>
          <Box
            sx={{
              p: 4,
              borderRight: { md: "1px solid #eee" },
              height: "100%",
            }}
          >
            <Stack spacing={3}>
              <Avatar
                src="https://randomuser.me/api/portraits/women/68.jpg"
                sx={{ width: 80, height: 80 }}
              />

              <Box>
                <Typography fontWeight={700}>
                  Lerato Mokoena
                </Typography>
                <Typography color="text.secondary">
                  Software Engineer @ TechWave
                </Typography>
              </Box>

              <Chip label="Mock Interview Session" />

              <Typography fontSize={14} color="text.secondary">
                45-minute 1-on-1 session focused on technical interview prep.
              </Typography>

              <Typography fontWeight={600}>
                R250 / session
              </Typography>
            </Stack>
          </Box> <
    /Grid>
    
    { /* RIGHT SIDE (CALENDAR + SLOTS) */ }
    <Grid item xs={12} md={8}>
          <Box sx={{ p: 4 }}>
            <Typography fontWeight={700} mb={2}>
              Select a Date & Time
            </Typography>

            {/* DATE SELECTOR */}
            <Stack direction="row" spacing={1} mb={3} flexWrap="wrap">
              {["10 May", "11 May", "12 May"].map((d, i) => (
                <Button
                  key={i}
                  variant={
                    selectedDate === d ? "contained" : "outlined"
                  }
                  onClick={() => setSelectedDate(d)}
                >
                  {d}
                </Button>
              ))}
            </Stack>

            {/* TIME SLOTS */}
            <Grid container spacing={2}>
              {timeSlots.map((time) => (
                <Grid item xs={6} sm={4} key={time}>
                  <Paper
                    onClick={() => setSelectedTime(time)}
                    sx={{
                      p: 2,
                      textAlign: "center",
                      cursor: "pointer",
                      border:
                        selectedTime === time
                          ? `2px solid ${PRIMARY}`
                          : "1px solid #eee",
                      "&:hover": { boxShadow: 2 },
                    }}
                  >
                    <Typography>{time}</Typography>
                  </Paper>
                </Grid>
              ))}
            </Grid>

            {/* CTA */}
            <Box mt={4}>
              <Button
                fullWidth
                variant="contained"
                disabled={!selectedTime}
                sx={{
                  backgroundColor: PRIMARY,
                  py: 1.5,
                  borderRadius: 3,
                }}
              >
                Continue to Booking
              </Button>
            </Box>
          </Box>
        </Grid> <
    /Grid> <
    /Box>
  );
};

export default BookingPage;